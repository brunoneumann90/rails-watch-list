<div class="container">
  <h1>
    <strong>*</strong>
    <%= @list.name %>
  </h1>
  <h3>Bookmarks</h3>
  <%= link_to 'Create a new bookmarker', new_list_bookmark_path(@list), class: 'btn btn-dark m-3'  %>
  <% @list.bookmarks.each do |bookmark| %>
    <div class="card">
      <p><%= bookmark.comment %>
      <%= image_tag(bookmark.movie.poster_url, size: '100x100') %>
      <%= link_to 'delete', bookmark_path(bookmark),
        method: :delete,
        data: {confirm: '😱 Are you sure?'},
        class: 'btn btn-danger btn-sm' %></p>
    </div>
  <% end %>
  <%= link_to 'Back', lists_path, class: 'btn btn-dark mt-3' %>
</div>
